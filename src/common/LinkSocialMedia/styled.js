import styled from "styled-components";
import { motion } from "framer-motion";

export const LinkSocialMedia = styled(motion.a)`
  text-decoration: none;
  font-size: 2rem;
  color: #e8eddf;
  cursor: pointer;
  padding: 0.5rem;
  background-color: rgba(255, 255, 255, 0.12);
  border-radius: 50%;
  width: 2rem;
  height: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
`;
